uses three for loops the first 2 checks every pair points and the last one checks 
all other points. then uses ax + by = c to calculate the line equation. it also checks
if a point was made to make the same line and skips it. then determines what side the line
is one and prints what points make a line. 