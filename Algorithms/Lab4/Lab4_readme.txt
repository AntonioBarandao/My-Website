loops through each point in the list,Compares the current point with every point after i
then calculates the distance using (x2 - x1)^2 + (y2 - y1)^2. checks if the distance is the smallest one so far
if distance < minDistance then prints the minDistance and squared distance