uses two while loops to sort numbers in non-decreasing order. The first one compares from the
beginging and the inner one compares neigboring numbers. for the comparing statment 
if the left number is bigger it swaps them by storing the left value in a temp variable 
moves the right variable over one then insert the temp variable in the correct position